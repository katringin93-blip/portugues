# Pipeline написания SEO-статей

> Открой этот файл в начале каждой сессии по созданию статей.
> Утверждён: 2026-04-14. Прожарен через Grill Me. Прошёл spec review.

---

## Предварительные требования (одноразовая настройка)

Перед первой статьёй убедиться:

- [ ] Создан `css/articles.css` (общие стили для статей)
- [ ] `research/` добавлен в `.gitignore`
- [ ] Скиллы установлены: seo-content-writer, content-quality-auditor, De-AI-fy
- [ ] Шрифты: JetBrains Mono + Geist (как на лендинге, Google Fonts CDN)
- [x] Яндекс Webmaster API: OAuth-токен получен и сохранён в `.env` ✅

---

## Обзор

Каждая SEO-статья проходит 5 шагов. Автоматизированы: написание, гуманизация, аудит.
Ручная проверка: только факты + финальный вид (HTML в браузере).

```
 1. ИССЛЕДОВАНИЕ
    │
    ▼
 2a. НАПИСАНИЕ ─── seo-content-writer
    │
    ▼
 2b. ГУМАНИЗАЦИЯ ─── De-AI-fy + SEO constraint
    │
    ▼
 2c. АУДИТ ─── content-quality-auditor (80 items CORE-EEAT)
    │
    ├── SHIP → идём на шаг 3
    ├── FIX  → правлю → показываю пользователю (шаг 3)
    └── BLOCK → правлю критическое → перезапускаю аудит (макс. 2 раза)
    │         └── 2 BLOCK подряд → показываю пользователю, решаем вместе
    │
    ▼
 3. ПОКАЗ ПОЛЬЗОВАТЕЛЮ
    HTML в браузере + отчёт (SEO score + факты с источниками)
    │
    ├── ОК → деплой
    └── Правки → исправляю → показываю снова (макс. 3 раунда)
              └── 3 раунда правок → пересматриваем тему/угол статьи
    │
    ▼
 4. ДЕПЛОЙ
    sitemap.xml + git push + переобход в Вебмастере
```

---

## Шаг 1: Исследование

**Инструмент:** Exa MCP (web_search_exa, web_search_advanced_exa).
**Fallback:** Если Exa недоступен — WebSearch. Сообщить пользователю о замене.

**Что делаю:**
- Поиск по русскоязычным и англоязычным источникам
- Официальные сайты (CAPLE, законодательство)
- Форумы и реальный опыт
- Конкуренты в топе по целевому запросу

**Дополнительно для грамматических тем:**
- Можно использовать учебники, включая «Basic Portuguese A Grammar and Workbook» (PDF в проекте)
- **ОБЯЗАТЕЛЬНО:** Перед использованием любого учебника как источника — спросить у пользователя разрешение
- Учебник = источник фактов и примеров, НЕ копировать текст дословно

**Артефакт:** `research/тема-facts.md` — справка с фактами и ссылками на источники.

**Правила:**
- Каждый факт — с источником (URL или название учебника + страница)
- Противоречия между источниками — отмечать явно
- Не выдумывать цифры — если нет данных, писать «не подтверждено»
- **ПРАВИЛО ПЕРВИЧНОЙ ВЕРИФИКАЦИИ:** Если факт ссылается на конкретную страницу (FAQ, документ, закон) — ОБЯЗАТЕЛЬНО открыть эту страницу (WebFetch) и подтвердить, что факт там действительно есть. Нельзя доверять вторичным источникам, которые пишут «confirmed by X» — нужно зайти на X и проверить самому. В research-файле отмечать:
  - ✅ **Проверено на первоисточнике** — открыл страницу, факт подтверждён
  - ⚠️ **Из вторичного источника** — так пишут prep2go/ciple.org, первоисточник не проверен

---

## Шаг 2a: Написание — seo-content-writer

**Скилл:** `~/.agents/skills/seo-content-writer/`

**Входные данные:**
- Файл-справка из шага 1
- Целевой ключевой запрос (primary keyword)
- 3-5 вторичных ключей
- Тип контента (guide / comparison / listicle)
- CTA: deep link на конкретный урок (см. правила CTA ниже)

**Что делает скилл:**
- Keyword map (primary в H1, first 100 words, H2, conclusion)
- Структура: H1 → H2 → H3, FAQ, таблицы
- Title (до 60 символов) + meta description (до 160 символов)
- Internal links: на `/` (лендинг) + другие статьи (если есть)
- External links: 2-3 авторитетных источника
- Self-check по 16 CORE-EEAT критериям

**Объём:** 1500-2000 слов.

**Артефакт:** `тема/index.html` (черновик).

---

## Шаг 2b: Гуманизация — De-AI-fy

**Скилл:** De-AI-fy (4 параллельных критика + рерайт)

**Критики:**
1. **Generic Detector** — AI-шаблонные фразы
2. **Rhythm Analyzer** — однообразная длина предложений
3. **Specificity Checker** — где добавить конкретику
4. **Fact Checker** — проверка claims через web search

**SEO constraint (добавляется к стандартным правилам):**
```
PRESERVE these SEO elements — do NOT rephrase or remove:
- Primary keyword in H1 tag (exact match)
- Primary keyword in first 100 words
- Primary keyword in at least one H2
- Meta description text
- FAQ questions and answers (Schema.org source)
- Internal links and their anchor text
```

**Правила рерайта:**
- Output ≤ original word count
- Vary sentence length (3-word и 20-word mix)
- Kill generic phrases
- Add 1 personal touch
- Break 1 grammar rule (начать с «И» или «Но», фрагмент)

**Артефакт:** обновлённый `тема/index.html`.

---

## Шаг 2c: Аудит — content-quality-auditor

**Скилл:** `~/.agents/skills/content-quality-auditor/`

**Что проверяет:**
- 80-item CORE-EEAT benchmark (8 измерений × 10 items)
- GEO Score (CORE) + SEO Score (EEAT)
- Veto checks (T04, C01, R10)
- Weighted score по типу контента

**Вердикты:**
| Вердикт | Действие |
|---------|----------|
| **SHIP** | Идём на шаг 3 (показ) |
| **FIX** | Правлю по рекомендациям → показываю пользователю |
| **BLOCK** | Правлю критическое → перезапускаю аудит (макс. 2 раза, потом эскалация к пользователю) |

**Важно:** После правок НЕ перезапускаю De-AI-fy. Правки точечные — показываю результат пользователю напрямую.

---

## Шаг 3: Показ пользователю

**Как:**
- Запускаю `py -m http.server 8080` в папке course
- Пользователь открывает `http://localhost:8080/тема/` в браузере
- Вижу финальный HTML со всеми стилями

**Отчёт (в чате):**
- SEO score из аудита
- Список ключевых фактов для проверки (5-10 пунктов с URL-источниками)
- Что было исправлено на шагах 2b-2c

**Пользователь проверяет:**
- Фактическая точность (по ссылкам на источники)
- Стиль и тон (не звучит ли как AI)
- Визуальный вид (картинки, таблицы, вёрстка)

**Решение:** ОК → деплой. Правки → исправляю → показываю снова (макс. 3 раунда, потом пересматриваем тему/угол).

---

## Шаг 4: Деплой

> **🔴 ОБЯЗАТЕЛЬНЫЙ ЧЕК-ЛИСТ ПЕРЕД `git commit`:**
> - [ ] Статья создана в `course/тема/index.html`
> - [ ] `sitemap.xml` обновлён (новый `<url>` + `lastmod` + `changefreq=weekly`)
> - [ ] **Ссылка на статью добавлена с лендинга `course/index.html`** (см. пункт 2 ниже) ⚠️
> - [ ] Всё стейджится одним коммитом, не по частям
>
> Если ссылка с лендинга не добавлена — **не коммитить**. Статья, висящая без входа с главной, не получает link equity и плохо индексируется.

1. Обновить `sitemap.xml` (новый `<url>` с `<lastmod>` и `<changefreq>weekly`)

2. **Добавить ссылку на статью с лендинга** (`course/index.html`) — КРИТИЧНО:

   **Почему**: корневой URL `/` имеет максимальный link equity на сайте. Без ссылки с главной новая статья висит изолированно — Googlebot и Yandexbot её находят только через sitemap, индексация и ранжирование заметно медленнее. Проверенный симптом: статья `/ciple-a2/` была деплойнута 14 апреля без ссылки с лендинга, ссылка появилась только 19 апреля отдельным заходом (5 дней потерянной индексации).

   **Как добавлять:**

   - **Фаза 1 (< 3 статей опубликовано)**: в блок `.further-strip` над футером `course/index.html`. Шаблон строки:
     ```html
     <a href="/тема/" class="further-strip__link">Заголовок статьи без кавычек</a>
     ```
     Добавить внутрь `<div class="further-strip__links">` следующим элементом.

   - **Фаза 2 (≥ 3 статей опубликовано)**: развернуть полную блог-секцию (дизайн в `план-seo-продвижения/блог-мокап.html`, секция "ФАЗА 2"). Удалить `.further-strip`, вставить новую секцию `.articles-section` с card-grid. Каждая новая статья — отдельная карточка `<a class="article-card">` с цветным бейджем рубрики (ЭКЗАМЕН / ВЫБОР ЯЗЫКА / ГРАММАТИКА / ПРАКТИКА).

   **Проверить после**: лендинг `learn-portugues.ru/` скроллится до футера, ссылка на новую статью видна, клик открывает статью.

3. `git add` конкретных файлов (новая статья + обновлённый `sitemap.xml` + изменённый `index.html`). Не использовать `git add -A`.
4. `git commit` + `git push` (автодеплой на Cloudflare Pages, 30-60 сек).
5. Обновить счётчик статей в этом файле (секция «Счётчик статей и перелинковка»).
6. Если счётчик = 3, 6, 9... → напомнить пользователю:
   - о перелинковке между статьями,
   - при счётчике = 3 — о переходе на фазу 2 блог-секции на лендинге.
7. **Яндекс Webmaster API** — переобход:
   - URL новой статьи (приоритетная очередь)
   - URL главной `/` (так как её HTML изменился — добавилась ссылка)
8. Google: ping устарел (deprecated 2023). Google подхватывает изменения через `lastmod` в sitemap автоматически.

---

## Файловая структура

```
course/
  css/
    articles.css                    ← общие стили для всех статей
  research/                         ← справки с фактами (в .gitignore)
    ciple-a2-facts.md
    portugalskij-ili-ispanskij-facts.md
    ...
  ciple-a2/
    index.html                      ← SEO-страница → /ciple-a2/
  portugalskij-ili-ispanskij/
    index.html                      ← SEO-страница → /portugalskij-ili-ispanskij/
  ...
  sitemap.xml                       ← обновляется при каждом деплое
```

**research/** должен быть в `.gitignore` (см. Предварительные требования).

---

## HTML-шаблон статьи

Каждая страница — standalone HTML. Включает:

**Head:**
- Lunaris DS CSS-переменные (те же что на лендинге)
- `css/articles.css` (общие стили для статей)
- Шрифты: JetBrains Mono + Geist (Google Fonts)
- Яндекс.Метрика (счётчик 108518143)
- OG + Twitter Card мета-теги
- Canonical URL: `https://learn-portugues.ru/{url-slug}/` (транслитерация, напр. `ciple-a2`)
- Schema.org: Article + FAQPage (JSON-LD)

**Body:**
- Header: лого + «← На главную»
- Article: prose-стили (H1, H2, H3, абзацы, таблицы, списки, blockquote)
- Картинки: Unsplash (утверждаются пользователем в ревью). Обязательно с описательным `alt` текстом
- CTA-блок (см. «Правила CTA» ниже)
- FAQ с аккордеоном (как на лендинге)
- Footer: такой же как на лендинге

### Конвенции языка и стиля

**Целевая аудитория** learn-portugues.ru — русскоязычные взрослые, планирующие эмиграцию в Португалию / работу / путешествия. Не IT-специалисты, не интернет-гики. Пишем на доступном русском.

**Запрещено:**
- **TL;DR** и другие английские интернет-сокращения (FOMO, YMMV, ICYMI и т.п.) — часть аудитории не поймёт. Использовать русские аналоги: "Главное кратко", "Коротко", "В двух словах", "Основное".
- Англицизмы без перевода, где есть рабочие русские слова (вместо "кейс" → "пример", "инсайт" → "наблюдение"). Исключение — термины, у которых перевод длиннее или хуже (visa, ПМЖ, SSL).
- Канцелярит ("играет важную роль", "в наше время") — это уже ловит De-AI-fy (Generic Detector), но лучше не писать изначально.

**Разрешено и желательно:**
- Имена собственные в оригинале (CIPLE, DELE, CAPLE, CCSE, D8, D7)
- Иноязычные термины языкового плана курсивом: *infinitivo pessoal*, *estou a comer*
- Цифры с источниками (Statista, Ethnologue, Jensen, FSI)

### Конвенции карточек (info-card / warning-card / success-card)

**Обязательная структура:**
```html
<div class="warning-card">
  <strong>Заголовок карточки</strong>                 ← первый strong, станет блочным
  Тело текста с возможным <strong>inline-bold</strong> или без. ← остаётся inline
</div>
```

**Правила:**
- **Первый `<strong>`** в карточке = заголовок. CSS применяет к нему `display: block` + `margin-bottom: 8px`.
- **Любой `<strong>` после** = обычный inline-акцент внутри тела. Ничего не ломается.
- **Не начинать карточку с текста до заголовка** — это ломает селектор `:first-child`, и заголовок не получит нужного стиля.
- **Не использовать `<b>` или `<em>` вместо заголовочного `<strong>`** — CSS заточен на `<strong>:first-child`.
- Тело карточки пишется прямо после заголовка (без обёрнутого `<p>`).

**История вопроса**: до 19 апреля 2026 селектор был `.warning-card strong { display: block }` — применялся ко ВСЕМ `<strong>` внутри карточки. Inline-bold в теле превращался в блочный элемент с отступом и визуально ломал верстку. Пофикшено через `:first-child`, комментарий в `css/articles.css`.

---

## Правила CTA (ссылка на курс)

Каждая статья содержит CTA-блок «Попробуйте в курсе». Ссылка зависит от темы:

**Если статья связана с конкретным уроком:**
- Ссылка ведёт на `/course/#/lesson/N` (где N — номер урока)
- Пример: статья про артикли → `/course/#/lesson/2` (Unit 2: Articles)
- После авторизации пользователь попадает сразу на нужный урок

**Если статья общая (CIPLE, «как выучить», сравнение):**
- Ссылка ведёт на `/` (лендинг)
- Лендинг сам ведёт на курс

**Маппинг статей → уроков:**

| Статья | Урок | URL |
|--------|------|-----|
| CIPLE A2 | общая | `/` |
| Португальский или испанский | общая | `/` |
| Артикли | Unit 2 | `/course/#/lesson/2` |
| Ser и estar | Unit 5 | `/course/#/lesson/5` |
| Спряжения | Unit 4 | `/course/#/lesson/4` |
| Числа | Unit 3 | `/course/#/lesson/3` |

> **TODO (техническое):** Проверить что SPA корректно обрабатывает deep link после авторизации — если пользователь не залогинен, показать авторизацию, потом перенаправить на `/lesson/N`. Если не работает — реализовать.

---

## Счётчик статей и перелинковка

**Опубликовано статей:** 2
- CIPLE A2 — 2026-04-15
- Португальский или испанский — 2026-04-19

> Обновлять счётчик после каждого деплоя. Когда счётчик достигает 3, 6, 9... — напомнить пользователю:
> «Опубликовано N статей. Пора обновить перелинковку — добавить ссылки между статьями.»

**Лог перелинковки:**
| Дата | Статьи перелинкованы | Кол-во ссылок добавлено |
|------|---------------------|------------------------|
| — | — | — |

---

## Решения из прожарки (Grill Me)

| # | Вопрос | Решение |
|---|--------|---------|
| 1 | Петля после аудита FIX | Правлю → показываю пользователю. Без повторного De-AI-fy |
| 2 | Внутренние ссылки | На лендинг `/` + между статьями. После 3-й статьи — перелинковка |
| 3 | Язык | Только RU. EN отложен |
| 4 | Картинки | Unsplash стоки, утверждает пользователь в финальном ревью |
| 5 | CSS | Общий `css/articles.css` |
| 6 | Объём | 1500-2000 слов |
| 7 | Верификация фактов | Отчёт с ключевыми фактами + ссылки на источники |
| 8 | Деплой | После «ок» пользователя — автоматический push |
| 9 | Первая статья | CIPLE A2, вторая — «португальский или испанский» |

---

## Порядок статей

| # | Страница | Ключевой запрос | URL |
|---|----------|-----------------|-----|
| 1 | Экзамен CIPLE A2 — подготовка | экзамен CIPLE, CIPLE A2 | `/ciple-a2/` |
| 2 | Португальский или испанский | португальский или испанский | `/portugalskij-ili-ispanskij/` |
| 3 | Курс европейского португальского (хаб) | курс португальского онлайн | `/kurs-portugalskogo/` |
| 4 | Фразы на португальском для жизни | фразы на португальском | `/frazy-na-portugalskom/` |
| 5 | Как выучить португальский | как выучить португальский | `/kak-vyuchit-portugalskij/` |
| 6 | Грамматика португальского | грамматика португальского | `/grammatika/` |
| 7 | Европейский vs бразильский | европейский бразильский португальский | `/evropejskij-vs-brazilskij/` |
| 8 | Грамматические (шаблонные) | артикли, ser estar, спряжения | шаблонные URL |

---

## Периодические задачи (отдельно от pipeline)

| Задача | Частота | Инструмент |
|--------|---------|-----------|
| SEO-аудит всего сайта | 1 раз/месяц | seojuice `audit` |
| Проверка индексации | 1 раз/неделю | Яндекс.Вебмастер (вручную) |
| Перелинковка между статьями | После каждой 3-й статьи | Claude |
| Обновление sitemap | При каждом деплое | Claude |

---

## Темп публикаций

**Цель:** 2-3 статьи в неделю (на старте, для быстрого набора контента).

При таком темпе:
- 8 статей из плана = ~3 недели
- Перелинковка: первый раунд после 3-й статьи (~конец 1-й недели)
- Далее по графику: после 6-й, 9-й и т.д.

---

## Яндекс Webmaster API (автоматический переобход)

**Статус:** ✅ Настроено (2026-04-14). Токен в `.env`, квота 150 URL/день.

**Использование при деплое (Claude читает `.env` и выполняет):**

```bash
# Загрузить переменные
source .env

# Переобход конкретного URL
curl -s -X POST \
  -H "Authorization: OAuth $YANDEX_WEBMASTER_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://learn-portugues.ru/{slug}/"}' \
  "https://api.webmaster.yandex.net/v4/user/$YANDEX_WEBMASTER_USER_ID/hosts/$YANDEX_WEBMASTER_HOST_ID/recrawl/queue"
```

---

## Установленные скиллы

| Скилл | Путь | Назначение |
|-------|------|-----------|
| seo-content-writer | `~/.agents/skills/seo-content-writer/` | Написание SEO-статей |
| content-quality-auditor | `~/.agents/skills/content-quality-auditor/` | 80-item CORE-EEAT аудит |
| De-AI-fy | Встроенный (CLAUDE.md / вручную) | Гуманизация текста |
| seojuice audit | `~/.agents/skills/audit/` | Аудит сайта целиком (ежемесячно) |
